import 'package:flutter/material.dart';


class LeaderElection extends StatelessWidget {
	const LeaderElection();

	@override
	Widget build(BuildContext context) {
		return const Scaffold(body: Center(
			child: Text('leader election')
		));
	}
}
