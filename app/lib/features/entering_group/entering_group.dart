import 'package:flutter/material.dart';


// think: a better name
class EnteringGroup extends StatelessWidget {
	const EnteringGroup();

	@override
	Widget build(BuildContext context) {
		return const Scaffold(
			body: Center(child: Icon(Icons.group))
		);
	}
}
